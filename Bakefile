#!/bin/bash

DIST=dist
ELEVENTY="bun -b run eleventy"

build() {
  $ELEVENTY
}

serve() {
  $ELEVENTY --serve --port 4000
}

deploy() {
  if [ -d $DIST ]; then
    lift deploy
  else
    err "'$DIST' does not exist."
  fi
}

clean() {
  rm -rf $DIST
}
